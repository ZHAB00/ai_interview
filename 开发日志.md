# AI 面试官 — 开发日志

**技术栈：** FastAPI + Vue 3 + MySQL + Redis + Docker  
**项目周期：** 2026-04-27 ～ 2026-05-02  
**来源：** 后端开发日志 / 前端开发日志 / 联桥日志

---

## 一、项目骨架搭建（第 1-2 天）

### 后端
- Pydantic Settings 配置系统（`.env` 读取）
- JWT 令牌生成/校验 + bcrypt 密码哈希
- 异步数据库引擎 (aiomysql) + SQLAlchemy ORM
- CORS 中间件 + 限流 (slowapi)
- 自定义异常类 + 全局异常处理器
- 6 个 ORM 模型：User / Resume / Interview / Question / Document / Report
- Alembic 数据库迁移
- 注册/登录/刷新令牌接口

### 前端
- Vue 3 + Vite + Element Plus + Pinia 脚手架
- 7 条路由 + beforeEach 鉴权守卫
- authStore：JWT 存储 + 401 自动刷新队列
- 登录/注册页（含表单验证）
- AppLayout + Footer 伦理声明

---

## 二、知识库与文字面试闭环（第 3-4 天）

### 后端
- 题库预置（19 道题，覆盖四个阶段）
- FAISS 向量存储 + Sentence Transformers 嵌入（BAAI/bge-small-zh-v1.5）
- RAG 混合检索（SQL 题库 + 向量文档）
- InterviewerAgent + CodeReviewAgent（DeepSeek LLM）
- WebSocket 实时通信（文字版）
- 会话管理（Redis Hash）
- 面试编排引擎（状态机：created → in_progress → completed）

### 前端
- DashboardPage：简历上传→解析遮罩→摘要→配置→快速体验
- 5 个 API service 文件
- 麦克风 + AudioWorklet 音频采集
- 能量 VAD + Silero VAD (onnxruntime-web)
- WebSocket 连接管理（3 次重连 + 15s 心跳）
- 面试房间：对话区 + 摄像头 + 控制栏
- Monaco Editor 代码编辑器（6 种语言语法高亮）

---

## 三、评分、报告与语音集成（第 5 天）

### 后端
- 五维度评分引擎（技术深度/广度/工程化/沟通/岗位匹配）
- 报告生成（含简历扣分 0-15 分）
- 阿里云 NLS 语音识别/合成 (STT/TTS)
- WebSocket 音频支持（二进制帧 + JSON 混合处理）
- 打断信令 + TTS 播放队列
- 沉默超时提示（15 秒）
- Feedback 反馈接口

### 前端
- ECharts 雷达图 + 错误解析报告页
- 音频播放管线（PCM16→WAV→Blob→Audio 元素）
- 答题流程：语音→STT→草稿气泡→编辑→发送
- 举报弹窗 + 面试回放

---

## 四、架构升级：简历驱动差异化面试（第 6 天）

- 废弃题库在面试中的使用，改为 LLM 根据简历动态生成题目
- 四个阶段差异化设计（初筛/HR面/技术面/终面各有 max_questions、max_follow_ups、strictness、question_guide）
- 技术面强制 3-4 层深挖，代码层硬限制防止 LLM 无限追问
- 岗位技能联动（Dashboard 选岗位自动匹配技能标签）

---

## 五、NLS → DashScope 千问实时语音迁移（第 7 天）

- 从阿里云 NLS 迁移到百炼 DashScope Qwen-ASR-Realtime + Qwen-TTS
- 协议：WebSocket → SDK SpeechSynthesizer/Recognition
- 热词定制（70+ 技术词汇提升 ASR 准确率 ~66%）
- TTS 从 SpeechSynthesizer 迁移到 MultiModalConversation（支持 qwen3-tts-instruct-flash）
- 修复采样率 24kHz→16kHz 音调异常

---

## 六、SMS 验证码 + 邀请码 + 密码管理（第 8 天）

### 后端
- CaptchaService：阿里云 SMS 发送验证码（60s 冷却，10 条/天限制）
- 邀请码：15 分钟 HMAC-SHA256 窗口，管理员管理
- 忘记密码：短信验证码重置
- Pydantic 校验中文化

### 前端
- RegisterPage 三步注册流程（邀请码→短信→密码）
- ForgotPasswordPage
- 邀请码展示弹窗（导航栏，15 分钟倒计时）

---

## 七、PTT（按住说话）模式重构（第 9 天）

核心问题：VAD 自动检测不可靠（TTS 回声自循环、截断过早、STT 误判）

改为 PTT 模式：
- 麦克风按钮三态（灰色禁用→蓝色可录→红色脉冲录制中）
- 松开按钮→STT→草稿气泡→4 秒自动发送
- 草稿可编辑（修改 STT 误判）
- 重新回答按钮
- 双重处理修复（speech_end 不再调 handle_text）
- 后台报告异步生成（独立 DB 会话）

---

## 八、音频播放管线五次重写（第 10-11 天）

| 尝试 | 方案 | 结果 |
|------|------|------|
| 1 | AudioContext (AudioContext.createBuffer) | resume() 悬挂 |
| 2 | `<audio>` 元素 | `play()` 被浏览器自动播放策略拦截 |
| 3 | AudioContext + await resume() | Chrome 中行为不稳定，suspended 时静默无声 |
| 4 | 持久化 `<audio>` + 静音预解锁 | 用户手势有效期内成功 |
| 5 | 最终方案：持久 `<audio>` + 直播 TTS Block→play |

---

## 九、安全审查与修复（第 12 天 / 2026-05-02）

对全栈进行安全审查，发现 27 项安全问题，修复 21 项代码级问题：

| 分类 | 数量 | 关键修复 |
|------|------|----------|
| 关键 | 5 → 2 已修 | API 密钥保护、.gitignore、上传目录认证下载 |
| 高 | 8 → 5 已修 | 数据库密码硬编码、Redis 无密码、Docker root 运行、Flower 无认证 |
| 中 | 8 → 4 已修 | CORS 收紧、is_disabled 检查、CSP 头部、Token 查询参数脱敏 |
| 低 | 6 → 4 已修 | python-jose→PyJWT、邀请码熵 32→48bit、异常日志、Vue DevTools 条件加载 |
| 手动 | 3 | SECRET_KEY 生成、API Key 轮换、限流启用 |

详见 `部署指南.md` 第八节踩坑录。

---

## 十、部署上线（第 13 天 / 2026-05-03）

服务器：2 核 4GiB / Ubuntu 22.04 / pandora.top

关键技术决策：
- 国内服务器用 `apt install docker.io`（官方脚本被墙）
- 配 4 个国内 Docker 镜像源（daocloud/registry.cyou/baidubce/nju）
- `.env` 统一管理所有环境变量（Docker Compose 默认读取）
- Nginx HTTP→HTTPS 重定向 + certbot 签发 Let's Encrypt 证书

详见 `部署指南.md`。

---

## 联桥排查速览（原 37 轮排查重点）

| # | 问题 | 修复 |
|---|------|------|
| 1 | Refresh Token Header→Body | 前端改用 Body 发送 |
| 2 | Feedback URL 不匹配 | 修正路径 |
| 3 | WebSocket 相对路径 | `http→ws` 推导 |
| 4 | resume_id 类型不匹配 | 改为可选 + 占位简历 |
| 5 | LLM JSON 解析无容错 | `json.loads` 加 try-except + fallback |
| 6 | `dict.get(key, default)` None 穿透 | 改为 `dict.get(key) or default` |
| 7 | 技术面无限追问 | 代码层硬限制 `max_follow_ups` |
| 8 | `ask_question` 绕过限制 | 补 `max_q` 硬拦截 |
| 9 | STT 结果未回显 | 新增 `user/text_echo` |
| 10 | 双重 `handle_text` | `speech_end` 移除直接调用 |
| 11 | TTS `extra_headers`→`additional_headers` | websockets 库 API 变更 |
| 12 | `_send_response` 顺序 | 先发文本，后生成 TTS |
| 13 | 录音 URL 丢失 | `user_audio_url` 赋值 + `db.commit()` |
| 14 | AudioContext 静默无声 | 最终改用持久 `<audio>` 元素 |
| 15 | ASR 模型名不存在 | `qwen3-asr-realtime` → `qwen3-asr-flash-realtime` |
| 16 | TTS 采样率不匹配 | 24kHz→前端 24000 修正 |
| 17 | 报告岗位串位 | `match_feedback` 校验岗位一致性 |
| 18 | `user/speech_end` 缺 `continue` → UnboundLocalError | 补 `continue` |
| 19 | 沉默提示太快 | 增加 `is_speaking` 等待 |
| 20 | 阶段首问重复显示 | `message` 与 `question` 字段分离 |
| 21 | NLS 识别技术词汇全挂 | 全栈迁移 Qwen-Realtime |
| 22 | Pydantic 校验错误未中文化 | 正则替换 8 条错误消息 |
| 23 | SMS 注册流程缺失 | 新增 captcha_service + API |
| 24 | 管理员登录无角色 | LoginResponse 补 role 字段 |
| 25 | celery extra_headers 不兼容 | 参数名修正 |

完整排查详细记录见 `联桥日志.md`。

---

*日志结束。项目已部署运行于 `https://www.aiview.pandahead.top`*
